{{if .NewFile}}
//generated by lazy
//author: seanlan

package service

import (
	"context"
	"{{.PackageName}}/{{.ModelPath}}"
)
{{end}}
func {{.ServiceName}}(ctx context.Context, req model.{{.ReqStructName}}) (resp model.{{.RespStructName}}, err error) {
	return
}